<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Timetables</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>

<div class="container mt-5">

    <div class="d-flex justify-content-between mb-4">

        <h2>Manage Timetables</h2>

        <a href="/admin/dashboard" class="btn btn-dark">
            Dashboard
        </a>

    </div>

    <div class="card shadow p-4 mb-5">

        <h4>Upload Timetable PDF</h4>

        <form
            action="/timetables/upload"
            method="POST"
            enctype="multipart/form-data"
        >

            <div class="mb-3">

                <label>Timetable Title</label>

                <input
                    type="text"
                    name="title"
                    class="form-control"
                    required
                >

            </div>

            <div class="mb-3">

                <label>Select PDF File</label>

                <input
                    type="file"
                    name="timetable_file"
                    class="form-control"
                    accept=".pdf"
                    required
                >

            </div>

            <button class="btn btn-warning">
                Upload Timetable
            </button>

        </form>

    </div>

    <div class="card shadow p-4">

        <h4>Uploaded Timetables</h4>

        <table class="table table-bordered mt-3">

            <thead>

                <tr>
                    <th>ID</th>
                    <th>Title</th>
                    <th>Uploaded By</th>
                    <th>PDF</th>
                    <th>Action</th>
                </tr>

            </thead>

            <tbody>

                <% timetables.forEach(timetable => { %>

                    <tr>

                        <td><%= timetable.id %></td>

                        <td><%= timetable.title %></td>

                        <td><%= timetable.fullname %></td>

                        <td>

                            <a
                                href="/uploads/timetables/<%= timetable.file_name %>"
                                target="_blank"
                                class="btn btn-primary btn-sm"
                            >
                                View PDF
                            </a>

                        </td>

                        <td>

                            <a
                                href="/timetables/delete/<%= timetable.id %>"
                                class="btn btn-danger btn-sm"
                            >
                                Delete
                            </a>

                        </td>

                    </tr>

                <% }) %>

            </tbody>

        </table>

    </div>

</div>

</body>
</html>